export enum MainRoutes {
  DASHBOARD = "/",
  DETAIL = "/details",
  STATISTICS = "/statistics",
  CONVERTER = "/converter",
  WALLET = "/wallet",
}

export enum PageTitle {
  DASHBOARD = "Dashboard",
  DETAIL = "Details",
  STATISTICS = "Statistics",
  CONVERTER = "BTC Converter",
  WALLET = "Wallet",
}